[
  {
    "file": "src/editor/markdown-common.ts",
    "rule": "incomplete-boundary-class",
    "count": 1,
    "reason": "REAL, UNFIXED. `WORD_CHAR_RE = /[\\p{L}\\p{N}]/u` drives CommonMark `_`-flanking; a combining mark is neither letter nor digit, so on NFD text the base letter of an accented cluster reads as a word edge and emphasis flanks in the wrong place. Adding `\\p{M}` changes markdown parse/serialize behaviour and needs round-trip + conformance coverage, so it is tracked as follow-up rather than slipped into a CI-guard PR."
  },
  {
    "file": "src/lib/in-page-find/matcher.ts",
    "rule": "incomplete-boundary-class",
    "count": 1,
    "reason": "REAL, UNFIXED, low severity. `WORD_RE = /[\\p{L}\\p{N}_]/u` backs the whole-word in-page find; on NFD text a trailing combining mark is read as a word boundary, so whole-word `cafe` matches inside `café`. This path only paints highlight ranges (visible, transient) — it never rewrites stored content — so it is tracked as follow-up rather than fixed here."
  }
]
