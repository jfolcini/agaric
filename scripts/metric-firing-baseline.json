[
  {
    "metric": "QueueMetrics::bg_deduped",
    "rule": "no-firing-test",
    "reason": "PROVEN, but not textually. src-tauri/agaric-engine/src/materializer/tests/idempotency.rs:133 binds `metrics().bg_deduped.load(..) > before` as `collapsed` and only records a dedup snapshot inside `if collapsed`; the `dedup_snap.expect(\"precondition: at least one drain must collapse duplicate tasks…\")` at :143 then fails the test if the counter never advanced within the retry budget. It cannot be asserted inline because the first non-collapsing attempt is legitimate (the two enqueues may land in separate drains). #3384 taught the guard to credit a bound firing comparison that an ASSERTION reads (`let collapsed = …; assert!(collapsed)`), which is exact — but this proof does not take that shape: it travels from `collapsed` through an `Option` assigned inside `if collapsed` and only then reaches `.expect()`, two hops through a mutable binding that a textual analysis cannot follow without also crediting `let rose = …; tracing::debug!(rose);`, which cannot fail. This entry goes stale (and must be deleted) the moment a firing assertion — literal or `assert!(collapsed)` — is added there."
  }
]
