{
  "$schema": "../gen/schemas/capabilities.json",
  "identifier": "default",
  "description": "Cross-platform capability for the main window. Desktop-only plugin permissions (global-shortcut, autostart, updater) live in desktop.json so mobile builds neither grant nor try to resolve them (#760).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    {
      "identifier": "opener:allow-open-url",
      "allow": [{ "url": "https://*" }]
    },
    "process:default",
    "clipboard-manager:allow-write-text",
    "os:default",
    "notification:default",
    "core:window:allow-set-title",
    "deep-link:default"
  ]
}
