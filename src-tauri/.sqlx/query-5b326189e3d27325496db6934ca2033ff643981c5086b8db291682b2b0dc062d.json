{
  "db_name": "SQLite",
  "query": "WITH RECURSIVE ancestors(cur_id, cur_type, depth) AS (\n              SELECT b.id, b.block_type, 0 FROM blocks b\n              WHERE b.id = ? AND b.is_conflict = 0\n              UNION ALL\n              SELECT parent.id, parent.block_type, a.depth + 1\n              FROM ancestors a\n              JOIN blocks child ON child.id = a.cur_id\n              JOIN blocks parent ON parent.id = child.parent_id\n              WHERE a.cur_type != 'page'\n                AND child.is_conflict = 0\n                AND parent.is_conflict = 0\n                AND a.depth < 100\n           )\n           SELECT cur_id AS \"cur_id!: String\"\n           FROM ancestors\n           WHERE cur_type = 'page'\n           ORDER BY depth ASC\n           LIMIT 1",
  "describe": {
    "columns": [
      {
        "name": "cur_id!: String",
        "ordinal": 0,
        "type_info": "Text"
      }
    ],
    "parameters": {
      "Right": 1
    },
    "nullable": [
      false
    ]
  },
  "hash": "5b326189e3d27325496db6934ca2033ff643981c5086b8db291682b2b0dc062d"
}
